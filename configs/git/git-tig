# Common settings for tig, a TUI Git repository browser
# https://jonas.github.io/tig/

[tig]
    # Speed up the main view by enforcing the default limit of loaded commits.
    main-options = --max-count=1000

    # Force UTF-8 characters for commit graph drawing.
    line-graphics = utf-8
