# Common settings for git custom pager: delta
# https://dandavison.github.io/delta/introduction.html

[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    dark = true
    line-numbers = true

    # Use 'n' and 'N' to move between diff sections
    navigate = true
